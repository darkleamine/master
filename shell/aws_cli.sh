#! /bin/bash
#pip is a package manager for Python that provides an easy way to install, upgrade, and remove Python packages and their dependencies.
# to install aws_cli il faut install python-pip
sudo apt-get update
sudo apt-get install python-pip
sudo apt-get install awscli
